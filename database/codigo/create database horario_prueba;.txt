create database horario_prueba;
use horario_prueba;
create table estudiantes (
id_estudiante int primary key,
nombre varchar(50),
correo varchar(50),
semestre int 
);

create table aulas(
id_aula int primary key,
tipo_aula enum("COMUN", "INFORMATICA", "LABORATORIO"),
capacidad int)

create table materias (
id_materia int primary key,
nombre varchar(50),
carga_horaria int);

create table profesores (
id_profesor int primary key,
nombre varchar(50),
correo varchar(50),
restriccion_horaria enum("MAÑANA", "TARDE", "NOCHE", "NINGUNA")
)
;

create table grupos(
id_grupo int primary key,
id_materia int,
id_profesor int,
constrait grupos_profesor_fk foreign key (id_materia) refences materias

);


create table grupos(
id_grupo int primary key,
id_materia int,
id_profesor int,
constraint grupos_materias_fk foreign key (id_materia) references materias(id_materia),
constraint grupos_profesores_fk foreign key (id_profesor) references profesores(id_profesor)

);